const int MAX = 10000;

// 加法函数
int add(int a, int b) {
    return a + b;
}

void print(int result) {
    putint(result);
    putch(10); // 打印换行符（ASCII 10）
}

int main() {
    int a[2];
    int i, t, n;

    a[0] = 0;
    a[1] = 1;
    i = 1;
    n = getint();
    if (n < 0) {
        putstr("ERROR!");
        putch(10);
        return 0;
    }
    if (n == 0) {
        print(a[0]);
        return 0;
    }
    if (n > MAX) {
        n = MAX;
    }
    print(a[0]);
    print(a[1]);
    while (i < n) {
        t = a[1];
        a[1] = add(a[0], a[1]);
        print(a[1]);
        a[0] = t;
        i = add(i, 1);
    }
    return 0;
}